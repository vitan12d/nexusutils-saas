# لوحة التحكم

## الدخول

افتح `/admin` وأدخل كلمة المرور (المحددة في متغير البيئة `ADMIN_PASSWORD`).

## الأقسام القابلة للتحكم

- **أخبار**: إضافة/تعديل/حذف مع عنوان، تاريخ، صورة، ملخص، ونص كامل.
- **مقالات**: مثل الأخبار.
- **فيديو**: عبر معرّف يوتيوب فقط.
- **روابط البث**: تظهر تلقائياً في شريط أعلى الموقع قبل المباراة بـ 15 دقيقة، وتختفي تلقائياً بعد انتهائها (بعد ~130 دقيقة من البداية).

> مهم: استخدم روابط القنوات/المنصات الرسمية المرخّصة فقط. روابط البث غير المرخّص تخالف سياسة AdSense وقد تعرضك للمساءلة القانونية.

## التخزين

البيانات تُحفظ في Vercel KV. لربطه:
1. في Vercel: Storage → Create → KV.
2. اربطه بالمشروع (يُضيف `KV_REST_API_URL` و `KV_REST_API_TOKEN` تلقائياً).
3. أعد النشر.

المحتوى في `content/*.json` يبقى كمحتوى أولي (seed) يُدمج مع ما تضيفه من اللوحة.
